set(ADWAITA_ASSET_DIR "${ADWAITA_THEME_DIR}/assets")
mark_as_advanced(ADWAITA_ASSET_DIR)

install(DIRECTORY custom DESTINATION "${ADWAITA_ASSET_DIR}")
install(DIRECTORY upstream DESTINATION "${ADWAITA_ASSET_DIR}")